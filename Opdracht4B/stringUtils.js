export function hoofdletter(str) {
    return str.toUpperCase();
}

export function reverse(str) {
    return str.split('').reverse().join('');
}

export function countChars(str) {
    return str.length;
}